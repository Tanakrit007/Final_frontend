# Bookshop Frontend (Tailwind + DaisyUI version)

- React + Vite
- TailwindCSS + DaisyUI (theme: corporate)
- Home page shows all products in one table from /api/items
- Navigation buttons to Books / Journals / Comics

## Run
```bash
npm install
cp .env.example .env
npm run dev
```
